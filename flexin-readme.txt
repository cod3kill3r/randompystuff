This is a simple quiz game which asks for a user to enter an option among the given options.
The expected input is an uppercase letter but the code allows user to enter an lowercase letter, which then later converted to uppercase and evaluates it.
